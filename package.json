{
  "name": "swarmy-ai-leads-engine",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port 3010 --strictPort --host",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && wrangler pages deploy"
  },
  "devDependencies": {
    "netlify-cli": "^27.0.3",
    "vite": "^5.4.2",
    "wrangler": "^4.118.0"
  },
  "dependencies": {
    "cloudflare": "^7.0.0",
    "dotenv": "^17.4.2"
  }
}
